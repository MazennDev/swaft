// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['randomuser.me', 'cdn.discordapp.com'], // Add cdn.discordapp.com here
  },
};

module.exports = nextConfig;
